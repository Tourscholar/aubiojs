{
  "name": "aubio-js-wasm",
  "version": "1.0.0",
  "files": [
    "build/aubio.js",
    "build/aubio.wasm",
    "build/index.d.ts"
  ],
  "main": "build/aubio.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "emcmake cmake -Bbuild -H. && make -Cbuild -j",
    "serve": "live-server --open=./app/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tourscholar",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
